server:
  port: 8501

spring:
  application:
    name: account-service

eureka:
  client:
    instance:
      hostname: localhost
    serviceUrl:
      defaultZone: http://localhost:8500/eureka/

spcloud:
  cassandra:
    contact:
      point: 192.168.99.100
    port: 9042
    keyspace:
      name: springcloud

    uname: cassandra
    pwd: cassandra
